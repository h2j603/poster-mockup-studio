# CLAUDE.md — poster-mockup-studio

> 클라우드/로컬 세션 공통 컨텍스트.

## 프로젝트

포스터 목업 생성 도구. **단일 HTML 파일** (`index.html`), 빌드 없음.

- 규격 프리셋은 캔버스 섹션에 위치, 실시간 비율 변경 지원

## 전역 규칙 (모든 세션 공통)

- 대화·커밋 메시지·코드 주석 모두 **한국어**. 커밋은 명령형.
- 작업은 feature 브랜치 → `main` PR 생성 → 머지까지 완료.
- 단일 파일 구조 유지 — 파일 쪼개지 않기.
- 디자인: 그림자·글로우·글래스모피즘 금지. 상세 → `.claude/rules/design.md`
- 작동하는 코드를 "개선" 명목으로 구조 바꾸지 않기.
